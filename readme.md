**Form input**

Provide a user with a form to fill basic salary and benefits
user fills the form and submits it
js extracts form values and performs computations with defined functions
display the output from js back to html




id attribute
an attribute that is used by js to uniquely 


document.getElementByid()

An inbuilt function used to access by JS to access and target html elements using the id


**JSfunctions**
1 . user interactivity
    the ability of a user to interact with an application. it gives the user the ability to perform actions whrnusing an application ---> anything a user can when using an application is called an event e.g scroll, click,zoom,hover,copy paste ,submit.
    **addEventListener** --> a function meant to wait for a end user's action(event ) and trigger js

    **function(event)** a function waits for an action triggers js script

2 . reactivity
    


    **document.getElementById().innerHTML ___Used 
